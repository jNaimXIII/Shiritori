{
    "name": "engine",
    "version": "0.0.1-alpha",
    "description": "Shiritori game engine.",
    "main": "src/main.ts",
    "scripts": {
        "build": "tsc",
        "dev": "nodemon --watch 'src/**/*.ts' --exec 'ts-node' src/main.ts"
    },
    "devDependencies": {
        "nodemon": "^2.0.22",
        "ts-node": "^10.9.1",
        "typescript": "^5.1.3"
    }
}
