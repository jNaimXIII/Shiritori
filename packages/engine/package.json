{
    "name": "engine",
    "scripts": {
        "build": "rimraf ./build && tsc",
        "dev": "nodemon --watch 'src/**/*.ts' --exec ts-node src/main.ts"
    },
    "devDependencies": {
        "nodemon": "^2.0.22",
        "rimraf": "^5.0.1",
        "ts-node": "^10.9.1",
        "typescript": "^5.1.3"
    }
}
